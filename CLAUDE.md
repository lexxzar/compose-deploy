# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Build & Test Commands

```bash
go build -o cdeploy .          # Build binary
go test ./...                   # Run all tests
go test ./internal/runner/ -v   # Run tests for a single package
go test ./... -count=1          # Run all tests uncached
go mod tidy                     # After adding/removing imports
```

The root command launches a Bubble Tea TUI requiring a TTY, so `cmd/root_test.go` tests flag registration and subcommand structure rather than executing the root command directly.

## Architecture

cdeploy is a dual-mode tool: interactive TUI (no args) or non-interactive CLI (`deploy`/`restart`/`stop`/`list`/`logs`/`exec` subcommands). Both modes share the same core through a three-layer architecture:

```
cmd/ (Cobra)  →  runner (pipeline orchestrator)  →  compose (docker CLI wrapper)
tui/ (Bubble Tea) ↗
```

Six subsystems carry their full rationale, failure modes and test pins in `docs/architecture/`; the paragraphs below carry the rules only. **Read the matching file before you change one of them** — each rule there exists because it was violated once.

- `docs/architecture/tui-help-overlay.md` — the `?` overlay, the container footer, the `q` back key
- `docs/architecture/tui-logs-and-search.md` — container search, log streaming, formatting, filter and search
- `docs/architecture/tui-inspect-screen.md` — the `i` inspect screen and `internal/compose/inspect.go`
- `docs/architecture/wait-snapshots-rollback.md` — the `--wait` gate, deploy snapshots, the rollback pipeline
- `docs/architecture/update-detection.md` — the `⇧` glyph, digest comparison, failure cascades, the TUI cache
- `docs/architecture/unmanaged-containers.md` — `HostContainers`, the picker row, the read-only UI contract

**Key abstraction**: `runner.Composer` interface (defined in `internal/runner/runner.go`, implemented by `internal/compose/compose.go` for local, `internal/compose/remote.go` for SSH, and `internal/compose/hostcontainers.go` for the read-only unmanaged-container view). This is the seam between the orchestrator and Docker — the runner is testable with mock implementations. Methods: `Stop`, `Remove`, `Pull`, `Create`, `Start`, `ListServices`, `ContainerStatus`, `ContainerStats`, `CheckUpdates`, `Logs`. When widening this interface, every mock implementation must add a matching no-op — there are mocks in `internal/runner/`, `internal/tui/app_test.go` (`mockComposer`), `cmd/deploy_test.go` (`opMockComposer`), and `cmd/list_test.go` (`mockComposer`, `mockComposerStatusErr`); a fast `grep -rn "func.*Composer.*ListServices" ./...` enumerates them.

**Event system**: `runner.Run()` sends `StepEvent` structs through a channel. The CLI consumer (`cmd/deploy.go:runOperation`) prints colored status lines. The TUI consumer (`internal/tui/app.go`) drives a Bubble Tea model through `tea.Cmd` that reads from the channel.

**TUI state machine**: Seven screens: `screenSelectServer` → `screenSelectProject` → `screenSelectContainers` → `screenProgress` (for operations), `screenLogs` (for log viewing), `screenConfig` (for config viewing/editing), or `screenInspect` (for the read-only container inspect view). The server screen is shown whenever servers are configured in `~/.cdeploy/servers.yml`; selecting "Local" fast-tracks to containers if a local compose file exists. The model is a flat struct with a `screen` field determining which view/update logic runs. Operations are triggered via action keys on the container screen (`r` restart, `d` deploy, `s` stop) which enter a confirmation sub-state (`confirming` bool + `pendingOp`) before proceeding. The `x` key execs into a running container interactively (enters confirmation with `pendingExec = true`, then shells out via `tea.ExecProcess`; shows warning if container is not running). The `l` key opens the log viewer for the cursor service without confirmation (read-only operation). The `c` key opens the config screen without confirmation (read-only by default). The `i` key opens the inspect screen for the cursor service, also without confirmation, and is the one container action key that is NOT gated on `m.readOnly()` — see the "Inspect screen" paragraph below. Back-navigation (`esc`, also aliased to `q` on nested screens — see "Exit confirmation for remote connections" below) resets downstream state; during confirmation, `esc` cancels without navigating back. The project picker is auto-skipped when the current directory contains a compose file and no servers are configured (backward compatible). `Compose.ListProjects()` discovers projects via `docker compose ls -a --format json`; both `ProjectLoader` literals in `cmd/root.go` then run that result through `projectsWithUnmanaged`, which appends the synthetic `(unmanaged)` row when the host has containers with no compose project label — see the three `Unmanaged containers — …` paragraphs below.

**Exit confirmation for remote connections**: `ctrl+c` is the hard-exit shortcut from any screen. When a remote SSH connection is active (`disconnectFunc != nil`), pressing `ctrl+c` shows a confirmation prompt instead of quitting immediately. The `quitting bool` field on Model gates a global intercept at the top of `handleKey()`: `y` quits, `n`/`esc` cancels, all other keys are swallowed. `View()` checks `quitting` first and renders a standalone confirmation screen via `viewQuitConfirm()` (`"Disconnect from {serverName}? (y/n)"`). The `tryQuit()` helper replaces direct `return m, tea.Quit` calls on all screens except `screenSelectServer` (which always quits immediately since no remote connection exists there). `quitting` is reset to `false` in `esc` back-navigation handlers that clear `disconnectFunc` and in the `entryLocal` handler. Local sessions (`disconnectFunc == nil`) bypass the prompt entirely — `tryQuit()` returns `tea.Quit` directly.

**`?` key-reference overlay**: `?` opens a full-screen key list for the current screen. It is a flag (`helpOpen bool` on Model), **never a screen constant** (back-navigation, `clearSearch()`, the session counters). `View()` checks it right after `quitting`, returning `viewHelp()`. `helpGroupsFor(s, hc helpContext)` (`internal/tui/help.go`) covers all 9 screens; the context bits travel in a NAMED `helpContext{canGoBack, readOnly, phase}`, never adjacent same-typed parameters. **Group ORDER is load-bearing**: `splitHelpGroups` cuts sequentially, so LEAVE must stay 4th of 6 on `screenSelectContainers` and 3rd of 4 in `readOnlyContainerGroups` and on `screenLogs` — do not reorder for readability. LEAVE is conditional (`leaveGroup(canGoBack)` renders `q quit` instead of `q esc → back`; `esc` stays BOUND on a standalone screen but is not a way out, so the group must not name it), and `Model.canGoBack()` is the single predicate behind BOTH it and the footer's `back` label. `screenProgress` is conditional on a THREE-state phase, since `esc` changes meaning per phase (running: `esc` CANCELS the op, `q`/`ctrl+c` no-op; waiting: `q`/`esc` skip the health wait; finished: they go back), so `Model.progressPhase()` MUST resolve in the SAME order as `viewProgress()`'s footer switch (`waiting` first, since it implies `done`); never one static table. `helpGroup.actions bool` marks groups whose keys are advertised nowhere else; `singleColumnOrder()` emits them first in the single-column fallback, FIND stays split out of MOVE so `/` and `n N` keep that flag, and a new action row moves every truncation threshold. `layoutHelpColumns`/`budgetHelpRows` clamp row width and count (`m.height` minus chrome, floor 1). **`viewHelp()` must NOT end its string in a newline, and the height tests must NOT trim one**: bubbletea keeps the LAST `m.height` of the `\n`-split `View()`, so one costs the title (`viewSelectContainers()`/`viewLogs()` too). **Intercept order in `handleKey()` is load-bearing**: (1) the `quitting` intercept, (2) the `helpOpen` swallow, (3) the `?` open, (4) the `q`→`esc` rewrite — the swallow MUST precede the open. Close keys are `?`, `esc`, `q`; every other key is swallowed. **`ctrl+c` clears `helpOpen` and FALLS THROUGH** instead of returning, so each screen's own `ctrl+c` semantics apply unchanged — including the mid-progress no-op. The open block is gated on `!m.typingInInput() && !m.confirmPromptArmed()`; **`confirmPromptArmed()` is screen-SCOPED** (`screenSelectContainers` → `m.confirming`, `screenSettingsList` → `m.settingsDelete`, else false) and a flat `!m.confirming` is WRONG — `enterProgress()` never clears `confirming`. `connectResultMsg` clears `m.helpOpen` on BOTH branches and `rollbackSnapshotMsg` clears it beside its `m.confirming = true`: **any future handler that arms a confirmation must do the same**. `typingInInput()` (`m.searching`; `m.logFiltering || m.logSearching`; `m.settingsField < 4` — that form, not `!= 4`) gates BOTH the `?` open and the `q` rewrite, so **a new screen that opens a text input needs one case there and nothing else**. Extend the key set in `TestHelpGroups_NamesEveryBoundKey` (and its read-only twin) whenever you bind a key — it fails BOTH directions — and move `TestAllScreens_Complete`'s bound to `int(<new last screen>) + 1` per screen added, or table tests skip it. **A key bound only in a sub-state needs a description that NAMES it**: `enter  confirm the prompt`.

**Container footer and the one-line width guard**: `containerFooter()` renders it, `containerFooterLines()` counts its physical lines; `svcVisibleCount()` reserves `2 + containerFooterLines()` and `containerFooter()` **pads every state out to that same count**. Both read `containerHelpLines()`, the single home of the idle pair (`back` via `canGoBack()`, read-only variant included). **The footer must advertise only keys the current state binds**: a committed search replaces `line2` wholesale with `n/N cycle • esc clear`, which is why `back` and `? keys` must live in `line1`; a TYPING search binds only `enter`/`esc`/`ctrl+c`, so the whole footer becomes `enter jump • esc cancel`. **The line COUNT stays state-independent** — decided from the idle pair alone, BEFORE any substitution, with the shorter states padded back by a trailing `\n` (the typing footer, and the confirm prompt). The one-versus-two-line guard measures `m.width >= ansi.StringWidth(oneLine)+2`, NOT `len(oneLine)+2` — **do not reintroduce `len()`**. A substitution can be WIDER than the idle `line2`, so `containerFooter()` clamps every line with `clampToWidth` rather than widening the count. Six tokens on the write path; the read-only pair carries four (`back`, `? keys`, `l logs`, `x exec`). Every other container key — `a`, `/`, `n N`, `i`, `c`, `U`, `R`, and `x` on the write path — lives in the `?` overlay only.

**`q` as back key on nested screens**: the 4th `handleKey()` intercept rewrites `q` to `esc` on nested screens; the existing `esc` handlers do the cleanup — **do not add parallel `q` handlers**. `q` quits directly only on root screens: (1) `screenSelectServer` always; (2) `screenSelectProject` when `len(m.servers) == 0 && m.config == nil`; (3) `screenSelectContainers` when `!m.showPicker && !m.confirming`. `screenProgress` while `!done && !failed` is a no-op for `q` (`ctrl+c` too) — an in-flight operation must not be cancelled. The rewrite is skipped when `m.typingInInput()` — `q` must reach an open input as a literal rune. `ctrl+c` is unchanged — `tryQuit()`, and the remote disconnect prompt. Full rationale and test pins: `docs/architecture/tui-help-overlay.md`.

**Backward-navigation state cleanup**: When navigating backward (`esc`) or handling errors (e.g. `connectResultMsg` failure), all mutable callbacks (`composerFactory`, `projectLoader`, `disconnectFunc`) must be explicitly cleared or restored to their local defaults (`localFactory`, `localComposer`, `localProjectLoader`). Bubble Tea's value-type model means stale closures silently persist across screen transitions. Every new callback field added to Model needs corresponding cleanup in: (1) `esc` from the project screen, (2) `esc` from the container screen, (3) `entryLocal` handler, (4) `connectResultMsg` error path.

**Stale async message guard**: Goroutines spawned by a screen (e.g., `readLogChunk` for `screenLogs`) may deliver messages after the user has navigated away. Message handlers for screen-specific async messages (`logChunkMsg`, `logDoneMsg`) must check `m.screen` and discard stale messages — otherwise they will read nil fields cleared by `esc` cleanup and panic. A monotonic `logsSession` counter provides additional stale-message rejection. This pattern applies to any future screen that spawns background goroutines.

**Service list scrolling**: The container select screen uses `svcOffset` to track the first visible service when the list exceeds terminal height. `svcVisibleCount()` calculates available lines: `m.height - headerLines - footerLines` (min 1), returning `len(m.services)` when `m.height == 0` for backward compat. Line counts account for lipgloss margins: `titleStyle` has `MarginBottom(1)` (adds a space-padded line), `helpStyle` has `MarginTop(1)` (adds a space-padded line), so header=3 (or 4 with column captions) and footer=3+ (not 2). A conditional column captions row ("Created", "Uptime") is rendered above the service list when any service has non-empty `Created` or `Uptime` data; `hasStatusColumns()` checks this and `svcVisibleCount()` adds 1 to `headerLines` accordingly. The captions use `descStyle` and are left-padded to align with data columns (10 + maxName offset). `fixSvcOffset()` ensures cursor visibility after any state change that affects visible count — cursor movement, window resize, confirming/warning state change. Scroll indicators (`▲ N more` / `▼ N more`) replace existing blank-line content between breadcrumb/services and services/help, so they never change the total line count used by `svcVisibleCount()`. `svcOffset` is reset to 0 alongside `svcCursor` in `servicesMsg` and `esc` back-navigation.

**Container search (search & jump)**: `/` is search-and-jump, **not a filter** — never narrow `m.services`, never touch `m.selected`; only `svcCursor` moves and matches highlight. *Committed* is `searchQuery != "" && !searching`; highlighting and `n`/`N` key off `searchQuery != ""` alone. `computeMatches` returns **ascending** indices (`nil` for an empty query) — `cycleMatch`'s wrap and off-match jumps rely on that ordering. **Ephemeral-on-departure invariant**: `m.clearSearch()` runs on **every** transition that leaves `screenSelectContainers` or reloads `m.services` — the 10 sites are `servicesMsg`, `esc` container→project, `esc` project→server, `entryLocal`, `connectResultMsg` error path, `enterLogs()`, `enterConfig()`, `enterProgress()`, `enterExec()`, `enterInspect()`. **Two-stage `esc`**: the guard at the top of `case "esc"` clears a committed search plus `fixSvcOffset()` and stays on screen, a second `esc` back-navs, and `esc` while typing restores `svcCursor` to `searchReturn` and consumes the key. Reopening `/` over a committed search calls `clearSearch()` FIRST; `searchInput` is built by `textinput.New()` in the `/` handler — **not** in `NewModel` (`Model{}` test literals). The bar line is **always reserved** (blank when idle, blank while confirming) — `viewSelectContainers()` writes it in both the `confirming` early-return branch and the normal one, but it costs NO extra row in `svcVisibleCount()`: it merges with `helpStyle`'s MarginTop blank, so counting it separately over-reserves a row. **One-physical-line invariant**: `searchBarLine()` MUST render exactly one physical line — size the typing `textinput.Width` to the pane and clamp with `clampToWidth()` (no-op at `m.width <= 0`); style only the raw name so width math is unaffected. The `q`→`esc` rewrite is skipped while `m.searching`, via `typingInInput()`.

**Log streaming / Log follow-aware auto-scroll**: `screenLogs` bridges the blocking `Composer.Logs()` to `logChunkMsg`/`logDoneMsg` via `io.Pipe`; the log ctx derives from `m.ctx`, and `esc` cancels it and clears all `logs*` fields. **ZERO new Model fields**: `YOffset` IS the pause state and "following" ≡ `m.logsViewport.AtBottom()`. `logChunkMsg` captures `following` BEFORE appending and calls `GotoBottom()` only when following; the three reformat sites (`w`, `p`, and `WindowSizeMsg`'s `screenLogs` branch) must EACH capture it before `fullReformat()` and re-`GotoBottom()` if following, and filter-change re-derives go through `rederiveLogs()` — the same capture around `fullReformat()`.

**Log formatting**: `w` soft-wrap (default on) and `p` JSON pretty-print (default off) are independent; pure formatters in `internal/tui/format.go`. `applyLogFormat()` folds only the not-yet-scanned tail (`foldNewRawLines`) into the cached `logsFormatted`; toggles and resizes call `fullReformat()`. Wrap off enables `SetHorizontalStep(4)`.

**Log search & filter**: `f` filters, `/` searches within the survivors. Predicates come from `buildMatcher(query, isRegex, allowNegate)` (`logfilter.go`) — `allowNegate=true` only for the filter (leading `!` inverts; search has no `!`), `valid=false` on empty query/lone `!`/bad regex, and a bad regex mid-type KEEPS the last-good predicate; regex-ness is read from the committed `logFilterCommittedRegex`/`logSearchCommittedRegex` **in `logFilterPred`/`logSearchPred`** (the `recomputeLogFilter`/`recomputeLogSearch` call sites still pass the live `IsRegex` flag, which is what makes `ctrl+r` take effect). **Raw-line pipeline**: `logsRawLines` is always unfiltered, `logsPartial` holds the trailing incomplete line, and `logsScanned` is a RAW-line cursor, **not** a survivor count — `foldNewRawLines` must ALWAYS advance it to `len(raw)`, or output duplicates. The filter runs at raw-line level BEFORE pretty-expansion. A terminal error goes to the filter-exempt `logsErrLine`, never into `logsRawLines`. **`setLogViewportContent()` is the single `SetContent` chokepoint** — route every write through it; `logsFormatted` stays UNSTYLED and highlighting is a SetContent-time overlay, foreground-only so `ansi.StringWidth` is unaffected. **`esc` ladder, in order**: typing-search → typing-filter → committed-search → committed-filter (both clear-only, stay) → leave screen; rungs 1-2 live in the two typing intercepts at the very TOP of `case screenLogs`, rungs 3-5 in the main-switch `esc`; `q` stays literal while an input is open, via `typingInInput()`. `logBarLine()` renders exactly ONE physical line and ends in `clampToWidth`. To reserve it, viewport height is `- 7` at **exactly the two log sites** (`enterLogs` and the `WindowSizeMsg` `screenLogs` branch) — the config sites keep `- 6`, so a blind find/replace is wrong. Full rationale and test pins: `docs/architecture/tui-logs-and-search.md`.

**Status refresh**: When returning from the progress screen or logs screen, `refreshStatus()` re-fetches `ContainerStatus()` via a `statusMsg` to update the running/stopped dots on the container screen. `ContainerStatus()` errors are propagated through `statusMsg.err` and `servicesMsg.err` and displayed via `svcErr`; a successful refresh clears any prior error. Follow this pattern whenever an operation changes container state.

**Server badge**: When a remote server is selected, the breadcrumb renders the server name via `serverBadge()`. Color is resolved via `resolveServerColor()` which returns the group color for grouped servers or the server's own color for ungrouped ones (nil-guarded for tests without config). A colored inline badge is shown only when the resolved color is non-empty; otherwise the breadcrumb falls back to the old plain server name (`cdeploy > prod-server > services`). `serverBadgeStyle()` in `styles.go` maps color names to lipgloss background colors. `serverHost` and `serverColor` fields on Model are set/cleared alongside `serverName` — follow the same cleanup discipline. No extra line is consumed, so no viewport height adjustment is needed.

**Config screen**: The `screenConfig` screen lets users view and edit docker-compose configuration files. Accessible via `c` key on the container screen (no confirmation). Uses a `ConfigProvider` interface (defined in `internal/tui/app.go`) with methods: `ConfigFile`, `ConfigResolved`, `EditCommand`, `ValidateConfig`. Both `Compose` and `RemoteCompose` implement it. The TUI type-asserts `composer.(ConfigProvider)` when entering — if the assertion fails (e.g., test mock), the `c` key is ignored. Key `r` toggles between raw file content and resolved output (`docker compose config`); resolved content is lazy-fetched on first toggle and cached. Key `e` shells out to `$EDITOR` via `tea.ExecProcess`; on return, re-fetches content and validates. Uses `configSession` (monotonic counter) for stale message rejection, matching the `logsSession` pattern. `esc` clears all `config*` fields and returns to the container screen. Local `findComposeFile()` probes `composeFiles` candidates; remote `findRemoteComposeFile()` uses a single SSH command to avoid multiple round-trips.

**Inspect screen**: `Inspector` (`Inspect(ctx, service) ([]byte, error)`) is declared in `tui` and type-asserted on the concrete composer, so a composer without it makes `i` a silent no-op — **keep the three `var _ Inspector = …` compile-time assertions in `app_test.go`**, one per concrete composer. **All three composers implement it, so `i` is NOT gated on `m.readOnly()`** — it reaches BOTH help tables through ONE shared append in `inspectGroup(readOnly bool)`, whose position is load-bearing for the `?` overlay's truncation order. **`docker inspect` is a top-level docker command, not a compose subcommand**, so all three bypass `command()`/`remoteCommand()`: `runDockerCmd`, `runRemoteDockerCmd` and the `dockerRunner` seam's `run`. ID resolution is a SECOND call: the two compose composers list `ps -a --format json` through `command()`/`remoteCommand()` into `resolveInspectID` (`parsePsEntries` tolerates array AND NDJSON), while `HostContainers` reuses `unmanagedEntries` + `pickHostInspectContainer`.; `errNoContainer(name)` is the ONE producer of `no container found for %q`. **`pickInspectContainer` must mirror `parseContainerStatus`'s Uptime gate exactly** — `State == "running" && uptime != ""` wins, longest running replica, a restarting one only when nothing running was seen; all-stopped falls back to the first matching entry, and only a service with no entry returns `("", false)`. **The parse layer is `internal/compose/inspect.go`**: `ParseInspect` unwraps docker's single-element array, strips `Name`'s leading slash, returns the narrow exported `InspectDoc` family — **add a field only together with the row that draws it**; `Inspect` returns raw `[]byte`, so raw mode stays byte-identical. `State.Health` and `Config.Healthcheck` are POINTERS — docker omits both keys with no healthcheck, and the HEALTH section drops on exactly that distinction (`hasHealthcheck` also drops `Test: ["NONE"]`). **`buildInspectSummary(doc, width, upd inspectUpdateInfo)` (`internal/tui/inspect.go`) stays pure** — the clock arrives inside `upd`, never `time.Now()`, and **its ZERO VALUE must draw no update row**. Sections render STATE, HEALTH, IMAGE, MOUNTS, ENV in that order; an empty one is omitted, STATE always renders, and HEALTH must build rows into a scratch `inspectBuilder` and write its header only when a row exists. IMAGE renders `image`, `image id` (NOT `digest` — it is the local image ID), `built`, `update`, `update id`, `update built`, `command`, `entrypoint`: the four update rows sit BETWEEN `image id` and `command` so the two ids and the two dates pair up, each keys off its OWN input, and **the section's presence gate is UNCHANGED** (a verdict alone never opens it). **Probe output soft-wraps and must NEVER truncate**: every line goes through `inspectBuilder`, which wraps with the cell-aware `wrapCells` (`ansi.Hardwrap(line, width, true)`) — do NOT use the rune-chunking `softWrapLine`, measure labels with `ansi.StringWidth`, and below `inspectMinValueWidth` stack the value under its label rather than shredding a rune per line. **Tabs are expanded BEFORE measurement, inside `wrapCells`** (`expandTabs`, 8-column stops) — expand-then-strip. **Every emitted line is sanitised at ONE chokepoint, `inspectBuilder.push`** (`sanitizeInspectLine` = `ansi.Strip`, then drop BEL, CR, DEL and the 8-bit C1 controls). A new section's content lines MUST go through `push`; only `section()`'s own styled title bypasses it (it carries a lipgloss escape sequence) — never route that title back through `push`. **Raw mode uses a narrower pass**: `setInspectContent()` writes `sanitizeInspectRaw(m.inspectRaw)`, which keeps newlines and must NOT run `ansi.Strip`; `m.inspectRaw` itself stays verbatim (filter at render only). ENV renders verbatim in both modes; masking, if added, must cover BOTH. **Mechanics**: `enterInspect()` bumps `inspectSession`, resets the seven `inspect*` fields, sizes through `inspectViewportSize()` (`m.height - 6` — NOT the logs `- 7` — 3-row floor; `m.width - 4`, `< 10 → 40` floor), calls `SetHorizontalStep(4)`, calls `m.clearSearch()` (departure site #10) and returns `fetchInspect()`; **the `WindowSizeMsg` branch must call that same helper.** `inspectDataMsg` is gated on screen AND `msg.session != m.inspectSession`, and treats a fetch that SUCCEEDED with zero bytes as an error. `setInspectContent()` is the single `SetContent` chokepoint and must `SetXOffset(0)` on every buffer change; the `r` toggle adds `GotoTop()`. `rebuildInspectSummary()` sets `inspectErr`, empties the summary and KEEPS `inspectRaw`, but must NOT flip `inspectShowRaw` — only the `inspectDataMsg` handler switches, once. `clearInspect()` is the single departure cleanup and does NOT bump `inspectSession`; `esc` returns WITHOUT `refreshStatus()`. The `r` case and the footer must read ONE predicate, `inspectToggleAvailable()`, never `len(inspectRaw) == 0`. The footer and the one-line error slot (`strings.Fields` + `clampToWidth`) must never wrap, and **`viewInspect()` must NOT end in a newline**. TUI-only: no `Composer` change, no ID field on `runner.ServiceStatus`. Full rationale and test pins: `docs/architecture/tui-inspect-screen.md`.

**Exec into container**: The `x` key on the container screen execs into a running container interactively. Uses an `ExecProvider` interface (defined in `internal/tui/app.go`) with method `ExecCommand(ctx, service, command) (*exec.Cmd, error)`. Both `Compose` and `RemoteCompose` implement it. The TUI type-asserts `composer.(ExecProvider)` when `x` is pressed — if the assertion fails (e.g., test mock), the key is ignored. If the service is not running, a warning is shown. Otherwise, confirmation is requested (`pendingExec = true`); on confirm, the TUI shells out via `tea.ExecProcess` which suspends Bubble Tea and gives full terminal to the exec process. On return, `execDoneMsg` resets `pendingExec` and calls `refreshStatus()`. Default shell command: `[]string{"/bin/sh", "-c", "command -v bash >/dev/null 2>&1 && exec bash || exec sh"}` (checks for bash first, falls back to sh for Alpine/minimal images; uses `command -v` instead of redirecting stderr on `exec` to avoid silencing bash's stderr for the entire session). Local: `docker compose exec <service> <command...>`. Remote: builds SSH command directly (like `EditCommand` pattern) with `-t` for TTY allocation — does NOT use `remoteCommand()`. CLI: `cdeploy exec <service> [-- command...]` with `--server`/`-s` support; follows `cmd/logs.go` pattern. `ExecCommand` returns `(*exec.Cmd, error)` (not executing it) so the caller controls execution. `DefaultExecCommand` is exported from `internal/compose/compose.go`.

**Docker Compose**: All docker interaction goes through `compose.Compose.command()` (local) or `compose.RemoteCompose.remoteCommand()` (SSH) which builds the correct command based on the `Standalone` field — `exec.CommandContext("docker", "compose", ...)` for plugin mode or `exec.CommandContext("docker-compose", ...)` for standalone mode. Both set `CURRENT_UID` env var (format `uid:gid` locally, `$(id -u):$(id -g)` on remote). Auto-detection via `Detect(ctx)` probes `docker compose version` first, then falls back to `docker-compose version`; the result is cached in the `Standalone` and `detected` fields. Detection is explicit — callers must call `Detect()` before first use; `command()`/`remoteCommand()` only read `Standalone`. `SetStandalone(bool)` allows inheriting the detection result into new instances. `SetTestHooks(run, output)` on both `Compose` and `RemoteCompose` injects test doubles. Exact subcommands: `stop`, `rm -f`, `pull`, `up --no-start`, `start`, `config --services`, `ps -a --format json`, `logs`, `exec`, `ls -a --format json`, plus top-level `stats --no-stream --format json` (NOT a compose subcommand — see below). Empty container slice means operate on all services. `parseContainerStatus()` handles both JSON array output (Docker Compose v2.21+) and NDJSON (older versions); it parses `CreatedAt` and `Status` fields from `psEntry` to populate `ServiceStatus.Created` and `ServiceStatus.Uptime`. `formatUptime()` in `internal/compose/uptime.go` converts Docker's Status string (e.g. `"Up 3 hours (healthy)"`) into compact form (`"3h"`): strips health suffixes, compacts time units (`hours` -> `h`, `days` -> `d`, etc.), handles special cases (`"About a minute"` -> `"~1m"`, `"Less than a second"` -> `"<1s"`), returns `"restarting"` for Restarting status, and empty string for non-Up statuses. `parseUptimeDuration()` converts compact uptime strings back to `time.Duration` for comparison (used to pick the longest-running replica among scaled services, which correctly handles restarts where CreatedAt is older but actual uptime is shorter). CLI subcommands reject `-a` combined with explicit container names.

**Remote SSH**: `RemoteCompose` wraps docker compose commands in SSH calls via a ControlMaster persistent socket (`/tmp/cdeploy-ctrl-{hash}-{pid}`). `ConnectCmd()` returns the SSH connect command for TUI's `tea.ExecProcess` (full terminal access for password prompts), `Connect()` runs it directly for CLI use, and `Close()` tears down the socket. All remote args are shell-escaped via `shellEscape()`. `CURRENT_UID` is evaluated on the remote host via `$(id -u):$(id -g)` to get the correct server-side UID/GID. The `Run()` function in `tui/app.go` calls `disconnectFunc` on exit to ensure socket cleanup. CLI uses `--server`/`-s` flag to select a server; TUI shows a server picker when servers are configured.

**Ad-hoc SSH (`-S`/`--ssh`)**: The `--ssh` persistent flag (registered in `cmd/root.go` alongside `--server`) is the ad-hoc complement to config-based `--server`. It accepts `[user@]host[:port]` and lets CLI users (especially CI) connect without a `~/.cdeploy/servers.yml` entry. Mutually exclusive with `--server`; requires `--project-dir`. Implemented via two helpers in `cmd/remote.go`: `checkRemoteMutex(serverName, sshTarget, identityFile)` enforces both the `--server`/`--ssh` mutex and the `--identity` requires `--ssh` rule at the top of each subcommand's `RunE`, and `resolveSSHRemote(ctx, sshTarget, projectDir, identityFile, newRemote)` parses the target via `config.ParseSSHTarget`, calls the injected factory (`opNewRemote`/`execNewRemote`/`logsNewRemote`/`newRemote` — preserving existing test seams), sets `RemoteCompose.SSHExtraArgs`, and runs `Connect()` + `Detect()`. On `Detect` failure the helper internally calls `Close()` before returning the error; on `Connect` failure cleanup is a no-op. The returned cleanup is always non-nil, so callers can `defer cleanup()` immediately after the call without nil-checking. Wired into `deploy`/`restart`/`stop`/`exec`/`logs`/`list` subcommands as a branch above the existing `--server` block — config-based path is unchanged. Parser lives in `internal/config/sshtarget.go`: rejects whitespace and IPv6 (`[`-prefixed); validates port `1..65535`; returns `SSHTarget{User, Host, Port}` with helpers `SSHHost()` (`user@host` or `host`) and `PortArgs()` (`["-p","NNNN"]` or nil). The `SSHExtraArgs []string` field on `RemoteCompose` is spliced **immediately before the host argument** in every SSH argv build site (Connect, Close, Detect plugin/standalone probes, `remoteCommand`, `findRemoteComposeFile`, `ConfigFile`, `EditCommand`, `ExecCommand`); nil = zero-behavior-change default. No TUI changes — `--ssh` is strictly a CLI feature.

**Ad-hoc SSH identity (`-i`/`--identity`)**: The `--identity` persistent flag (registered in `cmd/root.go` immediately after `--ssh`) accepts a path to an SSH private key, intended for CI/automation workflows that write a key from a secret and don't have a `~/.ssh/config` entry. Strict-narrow: only valid alongside `--ssh` (mutually exclusive with `--server` via the `--identity requires --ssh` rule); CLI-only (TUI mode rejects it). Path validation lives in `internal/config/identity.go` (`ParseIdentity`): trims input, rejects empty, expands `~/` paths via `os.UserHomeDir()`, rejects bare `~` and `~user` forms (only `~/...` is supported), requires `os.Stat` to succeed and report a regular file, requires `os.Open` to succeed (readability check beyond mode bits), and returns `filepath.Clean` of the resolved path. Relative paths pass through unchanged — `ssh(1)` resolves them against cwd. `resolveSSHRemote` appends `["-i", cleanPath]` to `SSHExtraArgs` after the port args, so every existing SSH argv splice site absorbs `-i` for free without any changes to `internal/compose/remote.go`.

**Config**: `internal/config` loads `~/.cdeploy/servers.yml` defining remote servers and groups. The `Config` struct has `Groups []Group` and `Servers []Server`. `Group` has `Name` and `Color` fields. `Server` has `name`, `host`, optional `project_dir`, optional `group`, and optional `color` fields. Color resolution: grouped servers inherit color from their `Group` via `Config.GroupColor()`; ungrouped servers use their own `color` field. `color` accepts ANSI color names listed in `config.ValidColors` (exported slice, single source of truth for both validation and the TUI cycle picker): red, green, yellow, blue, magenta, cyan, white, gray; invalid values are rejected by `Validate()`. `Validate()` also checks group name uniqueness, group color validity, and that servers reference existing groups. `Save(path string) error` writes config atomically (temp file + rename) with `MkdirAll` for parent dirs. When color is omitted (resolved or direct), the TUI keeps the old plain-text breadcrumb instead of rendering a badge. Servers with the same `group` are displayed together under a shared header in the TUI. `Load()` returns an empty config (not error) if the file doesn't exist; it also runs `migrate()` to auto-convert old-format configs (per-server colors on grouped servers) to the new format (colors on Group entries, first-server-wins for conflicts). SSH-specific options (keys, jump hosts, tunnels) belong in `~/.ssh/config` — except `-i` for ad-hoc CI/automation use via `--ssh`.

**Settings editor**: Two screens (`screenSettingsList` + `screenSettingsForm`) accessible via `s` key on the server select screen (only when `m.config != nil`). The settings list shows all servers in a table with Name/Host/Group/Color columns; color is resolved from the group for grouped servers (with a "(group)" indicator) or from the server directly. `a` adds, `enter`/`e` edits, `d` deletes (inline y/n confirmation). The form uses 4 `textinput.Model` instances (name, host, project_dir, group) and a cycle picker for color (left/right arrows cycle through `""` + `config.ValidColors`). `settingsField int` tracks focus (0-3 = text inputs, 4 = color picker). When a server has a group, the color picker shows a "(group)" label; cycling the color edits the group's color (affecting all servers in that group). Editing a grouped server loads `settingsColor` from the group; on save, `settingsColor` is applied to the group's color (not the server's). New group names auto-create `Group` entries; orphaned groups (no servers referencing them) are auto-cleaned on both save and delete. `resolveServerColor()` on Model returns group color when available, falling back to server color (nil-guarded for tests without config). Validation reuses `config.Validate()` on a temporary config (including `Groups`) before applying changes. On save: mutate `m.config.Groups` and `m.config.Servers`, call `m.config.Save(m.configPath)`, sync `m.servers = m.config.Servers`, rebuild `m.serverEntries` via `buildServerEntries()`. Config path and config are threaded into the TUI via `WithConfigPath(path)` and `WithConfig(cfg)` functional options — existing `NewModel()` call sites are not affected. State cleanup: `esc` from settings list returns to server select; `esc` from form returns to settings list with no data changes; `settingsErr` is cleared on transitions.

**Operations**: Four operations — `Restart` (stop → rm → create → start), `Deploy` (stop → rm → pull → create → start), `StopOnly` (stop), `Rollback` (stop → rm → create → start — Restart shape, **NO Pull**: registry outages are a classic rollback trigger, so offline rollback from local blobs must work). All share the same `runner.Run()` pipeline; the step list varies by operation type. `Steps()`/`buildSteps()` fold Rollback into the Restart branch (`default:` case, gated only by `op == Deploy` for the Pull step).

**Wait engine (`--wait` health gate)**: ONE implementation, `internal/runner/wait.go`, drives BOTH CLI and TUI. `EvaluateWait` is a **pure reducer** (no clock, no IO); seed `WaitState` via `NewWaitState(services)`. **Per-service precedence IS the spec, in this order**: (1) `Uptime=="restarting"` ×3, checked **FIRST** (it also reports `Running==false`); (2) unhealthy; (3) `Running==false` after `everRunning`; (4) never-ran ×5; (5) `Health!=""` ⇒ has a healthcheck, passes ONLY on `Health=="healthy"` (a "starting" service stays pending); (6) no healthcheck + running ⇒ after `Grace`. **Firm timeout boundary**: PASS in 5/6 is gated on `elapsed <= Timeout`, fail-fast 1-4 is NOT; anything pending at `elapsed >= Timeout` sweeps to timed-out. `WaitHealthy` resolves empty targets via `ListServices()`; **`context.WithDeadline(ctx, start+Timeout)` must be created FIRST, before ANY blocking call, covering that resolution AND the polls — no wait-phase call may outlive `--wait-timeout`**, each re-arm capped at `min(Poll, Timeout-elapsed)`. A deadline expiry sweeps via `state.reportTimedOut` (NOT nil-status `EvaluateWait`) → `(report, nil)` = exit 2; a PARENT cancel returns `ctx.Err()` raw → exit 1 — check `ctx.Err()` BEFORE `waitCtx.Err()`. Only OPERATIONAL failures error; a failing verdict is `(report, nil)`, `report.OK==false`.

**`WaitError` exit-2 contract**: `cmd.WaitError{Report, Err}` (`cmd/deploy.go`) wraps a non-OK **OR** errored `--wait` report; `main.go` `errors.As` → `os.Exit(2)`; `Unwrap()` must keep exposing the operational cause. `addWaitFlags` puts `--wait`/`--wait-timeout` on deploy, restart, rollback ONLY; `stop` never waits (`runOperationWithPrep` guards `op != runner.StopOnly`).

**Deploy snapshots & the state file**: every `Deploy` records each *running container*'s digest to `~/.cdeploy/state/<sha256(projectDir)[:12]>.json` **on the docker host**, BEFORE the pipeline stops anything. **Deploy-only** — never Restart, never Rollback; **captured pre-Stop** from the running container, NEVER the local tag; **merge-not-replace** (`mergeSnapshot`, fresh wins); the key hashes a NORMALIZED dir (`Abs`+`Clean` local, `path.Clean` remote); **warn-and-proceed** — capture and write failures never block. Schema version 1; any other returns typed `errSnapshotSchema`; `ReadSnapshot` returns `(nil, nil)` for a missing file. **Overwrite protection** (`existingForMerge`): ONLY a missing or `errSnapshotCorrupt` file may be overwritten; an `errSnapshotSchema` file or a transient read/SSH failure MUST abort the write. Local `WriteSnapshot` flocks `<state>.lock`; the REMOTE write is deliberately unlocked.

**Rollback pipeline & `PrepareRollback` prep / `ExtraComposeFiles` contract**: `cmd/rollback.go` reuses `runOperationWithPrep` with the `rollbackPrep` hook; never fork `runOperation`. Hook order: `ReadSnapshot` → `resolveRollbackTargets` refusals → `filterLiveTargets` against the CURRENT `ListServices()` (failure ABORTS prep — fail closed; a removed service is skipped under `-a`, errors when named) → `PrepareRollback`, which pulls absent blobs (a failed pull MUST abort BEFORE any override is written or the field set), writes `buildOverrideYAML` — `image: <repo>@<digest>` **plus `pull_policy: never`** per service, services in **SORTED order, never marshal a map** — and sets `ExtraComposeFiles = [main, override]`. Its cleanup (remove override, field to nil) MUST be deferred **function-scoped** — after the wait phase, BEFORE the remote SSH teardown, whose socket the `rm -f` still needs. `rollbackPrep` returns the resolved (snapshot ∩ live) set; `runOperationWithPrep` MUST thread it into **BOTH** `runner.Run` AND the wait phase — never the raw nil `containers`. Non-nil `ExtraComposeFiles` has `command()`/`remoteCommand()` splice `-f <f0> …` right after `docker compose`/`docker-compose`, BEFORE the subcommand, escaped on remote. `-f` disables auto-discovery, so the **main file MUST be first**, absolutized locally (`cmd.Dir = ProjectDir`). **A nil field must keep producing byte-identical argv.**

**TUI wait phase & `R` rollback (sessions + cleanup)**: `screenProgress`'s `waiting` sub-state drives a singleton `waitTickMsg` loop gated on screen+`waiting`+session; StopOnly never waits. Each poll MUST be `WithDeadline(m.ctx, m.waitDeadline)`-bounded and the `waitStatusMsg` error path MUST sweep pending→timed-out (`sweepWaitTimeout`) ONCE `elapsed >= Timeout` — before the deadline the failed poll is transient, state is carried forward and the loop keeps ticking; `esc` calls `clearWaitState()` (bumps `waitSession`). `R` **captures the selection into `rollbackTargets` at press time** (bumping `rollbackFetchSession`); that set — **NEVER live `selectedContainers()`** — drives validation, the prompt, `PrepareRollback` and `opContainers` (empty = ALL services), and an empty capture REFUSES. `rollbackSnapshotMsg` is screen+session gated, a `ListServices` failure fails closed, and a target absent from the snapshot or live compose is refused. The prep cleanup runs via `runRollbackCleanup` when **LEAVING `screenProgress` — NEVER goroutine-deferred**; a hard quit mid-wait must run it on the main goroutine too (both `ctrl+c` paths). `waitSession`/`rollbackFetchSession` bump at the same sites as the other counters; `waiting`/`waitState`/`waitDeadline`/`rollbackSnapshot`/`rollbackTargets`/`rollbackCleanup` clear at every departure site (esc chain, `entryLocal`, `connectResultMsg` error path); esc-from-progress must invalidate the update cache for `runner.Rollback` too. The TUI snapshots FIRST in the Deploy op goroutine; `Snapshotter`/`RollbackPreparer` are type-asserted. Rationale and test pins: `docs/architecture/wait-snapshots-rollback.md`.

**Health checks**: `ServiceStatus` has `Running bool`, `Health string` (values: `"healthy"`, `"unhealthy"`, `"starting"`, or `""` for no healthcheck), `Created string` (formatted creation time, e.g. `"2024-01-15 09:30"`), and `Uptime string` (compact uptime, e.g. `"3h"`, `"2d"`, or `""` if not running). For scaled services, `Running` uses OR (any running = running), `Health` uses worst-case priority (`unhealthy` > `starting` > `healthy`), `Created` uses the oldest replica, and `Uptime` uses the longest-running replica. The TUI shows health icons (♥/✗/~) next to status dots and Created/Uptime columns. The CLI `list` command shows the same icons and columns.

**Multi-project discovery**: The `list` subcommand auto-discovers all compose projects when no `-C` is specified (both local and remote). Services are grouped by project with headers. Per-project errors are non-fatal — a warning is printed to stderr and the project is skipped. When `-C` is specified, only that project's services are shown in a flat list. With `--stats`, CPU/Mem columns are populated by joining `docker stats --no-stream --format json` against each project's `ps` container IDs. The host-wide `docker stats` call is shared across every project: `runList` calls `compose.AllContainerStats`/`AllContainerStatsRemote` once before the project loop and passes the result map into `collectMultiProjectStats`. Each project's composer satisfies the `bulkStatsAggregator` interface (`ContainerStatsFromBulk`) and only runs `docker compose ps` per project, joining against the shared bulk map — one ~1.5s cost regardless of project count. Composers that don't implement the bulk interface (test mocks) fall back to per-project `ContainerStats()`; the fallback also runs if the host-wide fetch failed (`bulkStats == nil`), so stats degrade gracefully rather than disappearing.

**Resource stats**: `runner.ServiceStats` (`CPUPercent float64`, `MemoryUsed int64`, `MemoryLimit int64`) is populated from `docker stats --no-stream --format json` — host-wide NDJSON, one line per container, parsed by `parseStatsOutput()` in `internal/compose/stats.go` (tolerant of both NDJSON and JSON-array forms, mirroring `parseContainerStatus`). `ContainerStats()` is the **first method on `Compose`/`RemoteCompose` to bypass `command()`/`remoteCommand()`** because `docker stats` is a top-level Docker CLI command rather than a compose subcommand — calling `c.command(...)` would prepend `compose` and produce a malformed argv. `AllContainerStats()` builds `exec.CommandContext("docker", "stats", ...)` directly; `AllContainerStatsRemote()` builds the SSH argv directly, splicing `SSHExtraArgs` immediately before the host arg (same convention as `EditCommand`/`ExecCommand`, which also bypass `remoteCommand()`). Per-project `ContainerStats()` calls `docker compose ps --format json` for this project's container IDs, calls the bulk helper, joins by short container ID (first 12 chars), and sum-aggregates `CPUPercent`/`MemoryUsed`/`MemoryLimit` across replicas — a 3-replica service maxing each core shows ~300% CPU. Only running containers appear in the result map; stopped services are absent. Helpers `parseSize` (handles `B`/`KiB`/`MiB`/`GiB`/`TiB` binary and `kB`/`MB`/`GB`/`TB` decimal suffixes, case-insensitive), `parseCPUPercent` (strips `%`), and `FormatBytes` (exported; produces compact `124M`/`1.5G`) live in `internal/compose/stats.go` as the single source of truth — both `cmd/list.go` and `internal/tui/app.go` import `FormatBytes` from there. TUI fetches stats in parallel with status via `tea.Batch(refreshStatus(), refreshStats())` on every entry to `screenSelectContainers` and after every operation completes (return from progress / logs / exec). A periodic `refreshTickMsg` (every `statsRefreshInterval` = 5s, defined in `internal/tui/app.go`) keeps the data fresh while the user lingers on the screen: `Init()` kicks off a singleton `tea.Tick`, and the handler always reschedules itself so exactly one tick is in flight at any time regardless of how many transitions in/out of the screen happen. The handler gates fetches on `m.screen == screenSelectContainers && !m.confirming && m.composer != nil && !m.refreshInFlight` — anywhere else, the tick is a pure reschedule with no docker calls (off-screen / mid-confirmation / mid-disconnect / previous fetch still pending).

**Update detection — tri-state and Composer contract**: `runner.ServiceStatus.UpdateAvailable *bool` is a tri-state: `nil` = unknown / not checked / build-only / error, `true` = update available, `false` = current. `Composer.CheckUpdates(ctx, services)` returns ONLY checked services — an absent key must stay absent (that is the nil). **A non-nil error makes the map untrusted** — soft-failure consumers MUST discard it (`updates = nil`) and render all-blank-plus-warning. `compareImageDigestVia` returns `(updated, ok, err)`: **an empty digest is `ok == false`, never an error** (it would pollute the cascade counters).

**Update detection — digest comparison**: `parseLocalDigest` filters `RepoDigests` by repo (`stripTag`) and **falls back to the first non-empty digest when no repo matches**; `fetchRemoteDigestVia` tries `docker buildx imagetools inspect` FIRST and falls back to `docker manifest inspect --verbose` whenever imagetools yields no valid digest — a non-transport failure OR a successful call whose output the strict parser rejects; `errSSHTransport` short-circuits the fallback instead. — `errSSHTransport` propagates instead. **Do NOT add `--format '{{.Manifest.Digest}}'` back to `imagetoolsInspectArgs`** — silently ignored; `Name:` parses as the digest. `parseImagetoolsDigest` takes the top-level `Digest:` line and validates strict `sha256:<64 hex>`; do NOT add a "bare sha256 line" fallback. All three inspects **bypass `command()`/`remoteCommand()`** (top-level docker) — local `runDockerCmd`, remote `runRemoteDockerCmd` (`shellEscape`d image names, `SSHExtraArgs` before the host arg).

**Update detection — SSH transport classification (RemoteCompose only) / systemic-failure cascades**: `classifySSHError(err, stderr)` matches `sshTransportStderrPatterns` — SSH-ONLY signatures. **Do NOT add generic networking phrases** (`connection refused`, `no route to host`). A match wraps `errSSHTransport` and aborts `CheckUpdates`; anything else is a per-image failure → absent cell. Two post-scan cascades, both gated on `len(out) == 0`: **registry** when `networkFailures == remoteAttempts > 0` (`looksLikeNetworkErr`), **local-docker** when every local inspect failed AND every failure ALSO matches `looksLikeLocalDaemonErr` — that classifier gate is not optional. Partial failures never cascade; remote mirrors the registry cascade only. All three `CheckUpdates` share ONE loop, `scanImageUpdates`, memoized by image ref; **no cascade flag — the sentinel the comparer emits selects the cascade**: only `Compose` passes `wrapLocalImageInspectErr`, so only it emits `errLocalImageInspect`; `errSSHTransport` lives only on the remote runner.

**Update detection — TUI caching and refresh**: `updateCache` is keyed `projDir + "|" + serverName`; successes live `updatesCacheTTL` = 10m, failures `updatesErrorTTL` = 30s. **Errors ARE cached**, a new entry ALWAYS overwrites the prior one for that key (failure over stale success, success over failure), and **`errMsg` MUST be restored on every cache hit** (`maybeRefreshUpdatesCmd` and both replay paths). The `updatesMsg` handler applies its mutations (`updatesErr`, stale-glyph clears, hydration) **BEFORE the screen check**; only `fixSvcOffset` is screen-coupled. …deletes the current key's entry on a successful Deploy, Restart or Rollback, **before** the `m.done = false` reset; skipped on `m.failed` and on Stop. `U` bypasses the TTL but MUST keep its `if m.updateInFlight` guard, like `maybeRefreshUpdatesCmd`. Updates are **NOT** in the 5s `refreshTickMsg`.

**Update detection — sessions and in-flight discipline**: `updatesSession` gates `updatesMsg` and is bumped at the **same 7 sites** as `statsSession`/`statusSession` (project pick, esc container→proj, esc proj→server, `entryLocal`, `execDone`, return from progress, return from logs) but NOT on the `connectResultMsg` SUCCESS path; its ERROR path bumps `projectsSession` (and `rollbackFetchSession`) but NOT `statsSession`/`statusSession`/`updatesSession`. `updateInFlight` is cleared **BEFORE BOTH the session check AND the screen check, on EVERY arrival**, stale ones too. Every site that bumps `updatesSession` AND calls `maybeRefreshUpdatesCmd()` MUST reset `m.updateInFlight = false` first; the 2 leave-screen transitions reset it too. `connectResultMsg`'s error path clears `serverName`/`updatesErr`/`updateInFlight`; `entryLocal` clears `projDir`/`projName`/`updatesErr`.

**Update detection — rendering, CLI, and JSON**: `hydrateUpdates` (called from `servicesMsg`/`statusMsg`) writes verdicts into `m.svcStatus` and **MUST skip a verdict whose name is not already in `m.svcStatus`**. `updateGlyphStyle` appends `" ⇧"` (`compose.UpdateGlyph`); `maxName += 2` **unconditionally**, in the TUI and in `formatDots`/`formatDotsGrouped`, measured with `utf8.RuneCountInString`. Errors use the soft-warning slot prefixed `updates: `, priority `svcErr > statsErr > updatesErr`. `cmd/list.go` gates `CheckUpdates` behind `--updates` in BOTH single- and multi-project modes; a failure warns to stderr, SETS `updates = nil`, and never aborts the listing; JSON carries `update_available *bool` with `omitempty`. Full rationale and test pins: `docs/architecture/update-detection.md`.

**Update detection — inspect detail rows (`UpdateDetailer`)**: `UpdateDetails(ctx, services) (map[string]compose.UpdateDetail, error)` is declared in `tui` beside `Inspector` and type-asserted — **NOT a `Composer` change**, so the five mocks and `list --updates` stay untouched; keep the three `var _ UpdateDetailer = …` pins in `app_test.go`. Details ride `refreshUpdates()` — ONE Cmd, ONE `updatesMsg`, ONE `updateEntry`, ONE session — so the `⇧` triggers, TTLs, invalidation and `autoUpdatesAllowed()` gate apply by construction; **no new Model field, so no departure-site cleanup**. **A detail failure is DISCARDED, never `updatesMsg.err`** — a 429 matches `looksLikeNetworkErr` and would trip the registry cascade and blank the whole `⇧` column. Pass **ONLY the `true` verdicts, SORTED**, and skip the call entirely when none is true or `CheckUpdates` errored — `filterServices` reads an empty slice as **ALL** services, so the guard is a rate-limit rule, not an optimisation. Four round-trips per updated image (`localProbeArgs`, `manifestIndexArgs`, `rawManifestArgs`, `imageConfigArgs`), three of them registry; all bypass `command()`/`remoteCommand()` through the `dockerRunner` seam. `scanUpdateDetails` memoizes by image ref (**failures too**), visits services SORTED, returns at the first per-image failure, and short-circuits on `errSSHTransport` with the partial map. **Do NOT add `{{.Variant}}` to `localProbeFormat`** — an unknown template field is a hard error, killing step 1 on every image. `parseIndexPlatformDigest` is THREE-state: `hasIndex=false` → use the ORIGINAL ref; `hasIndex=true, found=false` → **ABORT this image**; every doubt (malformed JSON, non-list `manifests`, empty os/arch) returns the ABORT state, and `unknown` os/arch entries are attestations that must never be selected. `parseImageCreated` accepts BOTH the bare object and the platform-keyed map; `validImagetoolsDigest` is a WHOLE-STRING check. **`Unix() <= 0` is a sentinel, not data** (reproducible builds) — `parseImageTimestamp` and `formatInspectTimeValue` both drop the row. `rebuildInspectSummary()` is the ONE cache-lookup site (`m.inspectUpdateInfo()`, raw map read — NOT the TTL-checked lookup), and the `updatesMsg` handler MUST rebuild when `m.screen == screenInspect`, **on the failure path too**. Raw mode stays byte-identical. Full rationale and test pins: `docs/architecture/update-detection.md`.

**Session filters for stale responses**: `statsMsg`, `statusMsg`, `servicesMsg`, AND `projectsMsg` all carry a `session uint64` captured at fetch time; their handlers reject messages where the session (and screen) don't match. Three counters live on Model:
- `statsSession` covers `refreshStats` — bumped at every context change that swaps `m.composer` (proj pick, esc proj→server, esc container→proj, entryLocal, execDone, esc from logs/progress).
- `statusSession` covers both `refreshStatus` AND `loadServices` — bumped at the same 7 sites as `statsSession` (the lifecycle is identical: `loadServices` is the initial-load sibling of `refreshStatus`, fetching list + initial status in one shot).
- `projectsSession` covers `loadProjects` — bumped at every site that swaps `m.projectLoader` (entryServer, entryLocal, preselectedConnectMsg, connectResultMsg error path, esc proj→server). Without this, a stale `loadProjects` from server A's loader could overwrite `m.projects` after the user has switched to server B, then feeding B's `composerFactory` an A-discovered path on selection — potentially mounting the wrong directory on deploy.

Missing the session filter on any of the four is the same class of bug — codex's adversarial review caught each one in successive rounds. When adding a NEW async msg type that's tied to a screen context, give it a session and gate the handler on `m.screen != <its-screen> || msg.session != m.<sessionField>`.

**`refreshInFlight` prevents fetch stacking**: the tick fan-out `tea.Batch(refreshStatus, refreshStats, refreshTick)` schedules the next 5s tick *concurrently* with the current refresh — Bubble Tea batches execute concurrently, not sequentially. Without a guard, if `docker stats --no-stream` takes longer than 5s on a slow remote SSH hop, the next tick would stack another pair of fetches on top. The `m.refreshInFlight bool` is set true at every site that fires `refreshStats()` (the tick handler + the 5 transition handlers that fan out the fetch batch) and cleared in the `statsMsg` handler on any *current-session* arrival, *before* the screen check (success or error). Clearing has to happen before the screen check because if the user opens the config / logs / progress screen while a stats fetch is pending, the response arrives off-screen and would otherwise be discarded silently — leaving `refreshInFlight` stuck true and permanently silencing the periodic loop when the user returns to the container screen. The session-match condition makes the unconditional clear safe: stale responses (different session, indicating a context change has happened) don't touch the flag because the context-change handler already reset it. The flag is also explicitly reset to false at the 2 "leave screen" transition handlers (esc back to project picker, esc back to server picker) so the new context starts clean. When the next tick fires while the guard is set, it reschedules without fetching.

**Test seam for the tick**: `Model.tickCmdOverride func() tea.Cmd` is nil in production; tests set it via `installFakeTick(&m)` (in `internal/tui/app_test.go`) to swap the real `tea.Tick` for a non-blocking Cmd that returns a `fakeTickMsg`. Without the override, every test exercising the gated paths (off-screen, in-flight, confirming) would leave a real 5s `tea.Tick` goroutine running until the program exits. With the override installed, tests assert directly on whether `cmd()` returns a `tea.BatchMsg` (positive path: fetch batch) or any other msg (gated path: just the rescheduling tick). NewModel sets `m.refreshInFlight = true` for the local fast-track because `Init()` will fire `refreshStats`; tests that exercise the unblocked tick path must explicitly clear it.

When adding a new screen that should NOT trigger periodic refreshes, no change is needed: the gate already excludes any screen != `screenSelectContainers`. When adding a new screen that DOES need its own periodic refresh, prefer a separate tick (different msg type) rather than overloading `refreshTickMsg`. When adding a new context-change handler (anything that swaps `m.composer`), bump BOTH `statsSession` AND `statusSession`, and set `refreshInFlight` (true if the handler fires a refresh batch, false if it just navigates away). `statsMsg` carries a `session uint64` captured at fetch time from `m.statsSession`; the handler rejects messages where `msg.session != m.statsSession` in addition to the screen check, mirroring the `logsSession`/`configSession` pattern. `statsSession` is bumped on every context change that swaps `m.composer` (project pick, esc back to project picker, esc back to server picker, entryLocal) so an in-flight fetch from project A cannot land in project B's view. Soft failure: on stats fetch error, both CLI (`cdeploy: stats unavailable: <err>` to stderr, exit 0, blank cells) and TUI (`statsErr` rendered as soft warning) keep status — the load-bearing primary view — intact; in the TUI, when both `svcErr` and `statsErr` are set, `svcErr` wins (the more important failure).

**Ports column**: `ServiceStatus.Ports []runner.Port` (with `Host`, `HostPort`, `ContainerPort`, `Protocol` fields) is populated by `parseContainerStatus()` from `docker compose ps --format json`. Primary data source is the `Publishers []` array (Compose v2); when absent or empty, `parsePortsString()` falls back to parsing the `Ports` text field (e.g. `0.0.0.0:8080->80/tcp, :::8080->80/tcp`). Publishers with `PublishedPort == 0` (i.e. `expose:`-only/internal) are skipped. Dedup is keyed on the full tuple `(Host, HostPort, ContainerPort, Protocol)` so distinct bind interfaces on the same port (e.g. `127.0.0.1:8080->80` and `192.168.1.10:8080->80`) both survive. A separate `collapseIPv6Mirrors()` pass collapses only the IPv4/IPv6 *wildcard* mirror pair — when both `0.0.0.0` (IPv4 unspecified) and `::` (IPv6 unspecified) appear on the same `(HostPort, ContainerPort, Protocol)` tuple, the `::` entry is dropped. All other host strings are distinct user-visible binds: IPv6 loopback `::1`, link-local `fe80::1`, or explicit IPv6 addresses like `2001:db8::1` survive intact, even alongside an IPv4 sibling. Multiple non-wildcard IPv6 entries on the same tuple do not collapse against each other (full-tuple dedup in `mergePort` already removes exact repeats). Bracketed IPv6 like `[::]:8080` is normalized (brackets stripped on input). Empty `URL` from Compose is normalized to `0.0.0.0`. Scaled-service ports go through the same dedup + collapse + sort pipeline (sorted ascending by `HostPort`, then `ContainerPort`, `Protocol`, `Host`). Render helpers live in `internal/compose/ports.go`: `FormatPort()` returns `host:hp→cp` for IPv4 and `[host]:hp→cp` for IPv6 (the arrow is U+2192, not `->`; IPv6 hosts — any host containing `:` — are wrapped in brackets to disambiguate the host:port boundary), with `/proto` suffix only when protocol is non-empty and not `tcp` (`/udp` and `/sctp` are shown). Wildcard hosts (`0.0.0.0` and `::`, both meaning "all interfaces") are omitted entirely so the common all-interfaces case renders as a bare `hp→cp`; non-wildcard binds (loopback, explicit address) keep their host so the LAN-vs-loopback distinction stays visible. The wildcard-hiding affects only display — JSON output (`cmd/list.go` serializes the `Port` struct directly) preserves the original `host` field. `FormatPorts()` comma-joins. Both the CLI `list` (`formatDots`/`formatDotsGrouped` width-track `maxPorts` and append the column after Uptime when any service has ports) and the TUI container-select screen (captions row gains `"Ports"` via `hasStatusColumns()`) render the column. Stopped containers and services without published ports show a blank cell (padded whitespace for alignment). CLI JSON output includes a `ports` array (`omitempty`) per service with `host`/`host_port`/`container_port`/`protocol` fields.

**Unmanaged containers — the `HostContainers` composer and the three-method `dockerRunner` seam**: a container with no `com.docker.compose.project` label gets `compose.HostContainers` (`internal/compose/hostcontainers.go`), a read-only `runner.Composer`: five writes `return errReadOnly`, five reads go through the unexported `dockerRunner` field. **The seam needs THREE methods, not one builder**: `run` captures and classifies — the remote form reuses `runRemoteDockerCmd`, so `errSSHTransport` classification comes free; `stream` must NOT allocate a TTY and MUST wire BOTH Stdout and Stderr to `w`; `tty` MUST splice `-t` remotely. All three remote forms go through `RemoteCompose.remoteDockerCmdString(args)` — one home for `docker` + `shellEscape`. `hostPsArgs` = `ps -a --size=false --format {{json .}}`: the **template** form, never the bare `json` keyword (CLI >= 23.0 only), and **never drop `--size=false`**. Never name fields individually instead: an unknown field is a hard template error (`.State` needs CLI >= 20.10). `isComposeManaged` matches the key **only at a token start** (`HasPrefix(labels, k) || Contains(labels, ","+k)`), **never** by splitting `Labels` into `k=v` pairs — a value may contain a comma; the const keeps its trailing `=`, excluding the sibling `…config_files`/`…working_dir` keys. `parseHealthFromStatus` reuses the end-anchored `healthSuffixRe` from `uptime.go` — extend it, never duplicate it. `Running` is `State == "running"` and **must keep the `HasPrefix(Status, "Up ")` fallback for an empty `State`**. `ContainerStats` uses the seam directly and **early-returns before the stats call when there are no unmanaged entries**; a container in `ps` but absent from `stats` is **skipped, not an error**; `ContainerStatus` has NO such guard and **must not grow one**. `hostStatsArgs` takes the same form; that equivalence is **per-command**. `CheckUpdates` maps name→image from the `Image` field `ps` already returned and calls `scanImageUpdates` with a **nil `localErrWrap`** (no local-daemon cascade); `hostImageMap` drops empty refs and bare image IDs.

**Unmanaged containers — the picker row, the widened `ComposerFactory`, and the cache-key collision**: `compose.Project` gains `Unmanaged bool` and `Desc string` — do NOT overload `Status`. `UnmanagedProjectName`/`CountUnmanaged`/`WithUnmanagedRow` live in `internal/compose/unmanagedrow.go`; the row is appended **after** `ListProjects` and **bypasses `sortProjects`**; a `CountUnmanaged` error is **swallowed**. `viewSelectProject` renders `proj.Desc`, never `ConfigDir`, on that row. **Every branch `cmd/root.go` contributes lives in a package-level helper, never in the `RunE` closure**: `projectsWithUnmanaged`, `localComposerFor`, `remoteComposerFor`. **`ComposerFactory` is `func(proj compose.Project) runner.Composer`** so it can branch on `proj.Unmanaged`. **`updatesCacheKey()` MUST carry the `"unmanaged|"` prefix derived from `m.readOnly()`** — the key is `projDir + "|" + serverName`, and an unmanaged row's empty `ConfigDir` otherwise collides with the local fast track's bare `"|"`. Derive it from the COMPOSER, never a parallel `projUnmanaged bool` field.

**Unmanaged containers — the read-only UI contract**: `ReadOnlyComposer` is declared in `tui` and type-asserted on the concrete composer; it needs a **named** `ReadOnlyComposer() bool` method — never a method-less marker; `Model.readOnly()` is nil-safe. `d`, `r`, `s`, `R`, `c`, `space` and `a` all early-return, **each calling `fixSvcOffset()` first**. `c` and `R` need an **explicit** gate anyway. **Nothing advertises a no-op**: `containerHelpLines()` returns a read-only idle pair (`line1` drops `space toggle`, `line2` = `l logs • x exec`) read by both `containerFooterLines()` and `containerFooter()`; the line count **must stay state-independent** and every rendered line is clamped with `clampToWidth`. `helpGroupsFor` returns `readOnlyContainerGroups(canGoBack)` when `helpContext.readOnly`: SELECT and OPERATE go whole and `c` leaves INSPECT, but **`enter` MOVES into INSPECT** (`confirm the exec prompt`). FIND/INSPECT come from shared `findGroup()`/`inspectGroup(readOnly bool)` — add a key once, not twice. Drift pins run **per variant, never a union** — extend `TestHelpGroups_NamesEveryBoundKey` and its `ReadOnly` twin. The title drops the `(n/m selected)` counter and rows the `[ ]`/`[x]` checkbox, so the caption pad must be **7**, not **10**. **The update check must NOT fire automatically here**: all THREE entry points — `maybeRefreshUpdatesCmd()`, the `statusMsg` self-heal and `Init()`'s picker-skipped fast path — consult `autoUpdatesAllowed()` (= `!m.readOnly()`), leaving `U` the only trigger. `NewModel` sets `updateInFlight = m.autoUpdatesAllowed()`, never a flat `true`. Both gated paths also **clear `m.updatesErr` once the cache entry has expired** (the `statusMsg` clear sits BEFORE its `fixSvcOffset()`). Full rationale and failure modes: `docs/architecture/unmanaged-containers.md`.

## Adding New Operations

1. Add constant to `Operation` enum in `internal/runner/runner.go`
2. Update `String()`, `Steps()`, and `buildSteps()` in the same file
3. Add action key case in `screenSelectContainers` handleKey in `internal/tui/app.go`, AND name the key in the matching group in `helpGroupsFor()` (`internal/tui/help.go`) — the `?` overlay is the only place the full key table is advertised now that the footer carries 6 tokens
4. Gate the new key on `m.readOnly()` with an early return, and leave it OUT of `readOnlyContainerGroups()` (`internal/tui/help.go`). Every operation is a compose pipeline, and an unmanaged container has no compose file, so a new op is by definition inert on the read-only screen — a key the read-only table still named would advertise a no-op, which is the failure that whole variant exists to prevent
5. Add CLI subcommand in `cmd/deploy.go` and register in `cmd/root.go`
6. Tests: runner sequence test, cmd subcommand/flag tests, TUI action key + confirmation tests, plus the expected-key set in `TestHelpGroups_NamesEveryBoundKey` (`internal/tui/help_test.go`), which fails in both directions when the help table drifts from the dispatch. If the operation is a WRITE, add the key to the table in `TestReadOnly_GatesWriteKeys` (`internal/tui/app_test.go`) so the read-only screen is pinned to refuse the gate step 4 added. The read-only help pin needs NO edit for a gated key — it stays out of `TestHelpGroups_ReadOnlyNamesEveryBoundKey`'s hand-maintained bound set, and that test's names-nothing-unbound direction is what catches a table that advertises it anyway.

If the new operation needs an op-specific pre-run step (like Rollback's snapshot read + digest-pin prep), don't fork `runOperation` — route through `runOperationWithPrep(ctx, op, all, containers, prep)`. The `prep func(context.Context, runner.Composer) (func(), error)` runs AFTER the composer is built and BEFORE the pipeline (so it can apply refusal rules and configure the composer, e.g. set `ExtraComposeFiles`); its returned cleanup is deferred function-scoped, so it runs after the wait phase but before the remote SSH teardown (LIFO). deploy/restart/stop pass a nil prep and keep the original path. Rollback lives in its own `cmd/rollback.go` (see "Adding New CLI Subcommands") but is a deliberate reuse of this deploy machinery, not a fresh subcommand skeleton.

## Adding a New TUI Screen

**No failing test forces any of this.** The bound in `TestAllScreens_Complete` anchors to the LAST screen constant, so it has to be moved by hand every time; until it is, a screen missing from `allScreens` silently skips all six tests that iterate it. Three more tables promise completeness in their names and iterate a literal list instead, so they stay silent too. `screenInspect` (the 9th screen) is the worked example.

1. Append the constant to the `screen` iota in `internal/tui/app.go`.
2. Add it to `allScreens` (`internal/tui/help_test.go`) AND move `TestAllScreens_Complete`'s bound to `int(<newScreen>) + 1`.
3. Add the `screenName()` and `helpGroupsFor()` cases in `internal/tui/help.go`, and the `bound` row in `TestHelpGroups_NamesEveryBoundKey` — step 2 forces that row to exist (`screen %d has no expected key set`), and the pin then runs BOTH directions against the screen's `handleKey` case.
4. Add the `View()` case and, if the screen holds a viewport, a `WindowSizeMsg` branch — with the SAME width and height floors the `enter*` helper applies, or the two sizing sites disagree on a narrow pane.
5. Update the three hand-maintained tables that step 2 CANNOT reach — `TestCtrlCConfirmation_AllRemoteScreens` (`app_test.go`), `TestHelpOverlay_OpensFromEveryScreen` and `TestHelpOverlay_SwallowsEveryActionKey`'s `containerKeys` (`help_test.go`). Their names say "AllRemoteScreens" / "EveryScreen"; they iterate a literal list.
6. Add the entry key to the container dispatch, name it in the matching `helpGroupsFor` group, and call `m.clearSearch()` at the new `enter*` departure site (see the ephemeral-on-departure list in the container-search paragraph — the count in that sentence moves too).
7. Add a `typingInInput()` case ONLY if the screen opens a text input; that one case is all a new input needs, since the `q`→`esc` rewrite and the `?` open gate both read it.
8. If the screen fetches asynchronously, give it a session counter and gate its message handler on `m.screen != <its-screen> || msg.session != m.<its>Session` — see "Stale async message guard".

## Adding New CLI Subcommands

See `cmd/list.go`, `cmd/logs.go`, and `cmd/exec.go` for the pattern. Each subcommand:
1. Creates a `*cobra.Command` in its own `new*Cmd()` function
2. At the top of the `Run*` function, calls `checkRemoteMutex(serverName, sshTarget, identityFile)` to enforce the `--server`/`--ssh` mutex and the `--identity requires --ssh` rule (it must run before `os.Getwd()` and `signal.NotifyContext` so spurious setup doesn't run when the flags conflict)
3. Branches in this order: `sshTarget != ""` → `resolveSSHRemote(ctx, sshTarget, projectDir, identityFile, <factoryVar>)` (passing the subcommand's own injectable factory); else `serverName != ""` → load config, `FindServer()`, create `RemoteCompose`, `Connect()`, defer `Close()`, `Detect()`; else fall back to local `compose.New(dir)`
4. Is registered in `cmd/root.go` via `rootCmd.AddCommand()`

`cmd/skill.go` (the `skill install|show|uninstall` subcommand, see "Agent Skill distribution" below) is a deliberate exception to this pattern — it operates on local files only and has no `--server`/`--ssh`/`--project-dir`/remote wiring.

`cmd/rollback.go` (the `rollback` subcommand) is a second deliberate exception: rather than reimplementing the composer-branch skeleton, it reuses the deploy machinery via `runOperationWithPrep(ctx, runner.Rollback, …, rollbackPrep(…))` — the shared `runOperationWithPrep` already does the `checkRemoteMutex` → ssh/server/local branching, and `rollbackPrep` is the op-specific pre-run hook (snapshot read + refusal rules + digest-pin prep). See "Adding New Operations" for the prep-hook recipe.

## Agent Skill distribution (`cdeploy skill`)

**`skills/` package**: the repo-root `skills/` directory is BOTH the external-installer convention directory (so `npx skills add lexxzar/compose-deploy` and `gh skill install lexxzar/compose-deploy` find `skills/cdeploy/SKILL.md` on the default branch) AND a Go package holding the `//go:embed cdeploy` directive. `skills.FS` exposes the embedded tree; `skills.CanonicalSkill()` returns the raw, unstamped SKILL.md bytes — the single source of truth for the CLI, external installers, and verification, version-locked to the binary via `go:embed` (panics on a missing embed, since that is a build/packaging fault, not a runtime error to thread).

**`cdeploy skill install|show|uninstall <claude|codex|all>`** (`cmd/skill.go`): installs the embedded skill into agent skill directories. Targets resolve via `skillTargetDirs()`: claude → `~/.claude/skills/cdeploy` (also read by Cursor/OpenCode); codex → `~/.agents/skills/cdeploy` AND `$CODEX_HOME/skills/cdeploy` (default `~/.codex`; an EMPTY `$CODEX_HOME` is treated as unset because `t.Setenv` can set but never unset an env var); all → the deduped union. Dedupe (`resolveDedupeKey`) resolves symlinks on the deepest EXISTING ancestor of each candidate (the target dir usually does not exist yet, and `filepath.EvalSymlinks` errors on missing paths), then re-appends the not-yet-existing suffix, so two candidates pointing at the same physical directory through a symlink collapse to one write. Multi-path install/uninstall aggregate per-path outcomes — one failed destination never aborts the others, and any failure yields a non-zero exit. Per-agent pickup notes are printed to stdout after a successful install. `show` writes the raw unstamped canonical via `cmd.OutOrStdout()` (NOT `os.Stdout` — testable with `cmd.SetOut(&buf)`); it also enables `cdeploy skill show > .claude/skills/cdeploy/SKILL.md` for project-level placement.

**Stamp/hash ownership model**: install never trusts a file it did not write. `renderSkill(canonical, version)` injects a metadata block — `metadata:` header + `cdeploy-version` + `cdeploy-content-hash: sha256(canonical)` — immediately before the frontmatter's CLOSING `---` (the second `---`, found via `frontmatterCloseOffset`; never a bare `---` in the markdown body), so the frontmatter stays the first bytes of the file (Gemini silently skips otherwise). `stripOwned()` removes exactly that block, giving the round-trip invariant `stripOwned(renderSkill(canonical, v)) == canonical` as literal byte-equality; the stamped hash is therefore simply `sha256(canonical)`, no self-reference. `verifySkillFile(onDisk)` answers ONLY "is this file consistent with its OWN stamp?" → `intact` / `modified` / `unstamped` (stripOwned → sha256 → compare to the recorded hash). The install decision needs a SECOND, distinct check — `bytes.Equal(stripOwned(onDisk), canonical)` against THIS binary's canonical bytes — to reach `unchanged` vs stamp-refresh vs updated; without both checks the `unchanged` state is unreachable. Modified or unstamped files (e.g. a copy dropped by `npx skills`) are refused by both install and uninstall without `--force`; uninstall removes only the stamped `SKILL.md`, then the `cdeploy/` dir only when it is empty (never a recursive delete). Dev builds stamp `cdeploy-version: dev` — freshness keys on the content hash, not the version string.

**SKILL.md content test pins** (`skills/skills_test.go`): future edits to `skills/cdeploy/SKILL.md` MUST keep these satisfied — frontmatter is the very first bytes (`---\n` fence with a closing `---`), `name: cdeploy` (matches the directory AND the `^[a-z0-9]+(-[a-z0-9]+)*$` regex), `description` non-empty and ≤1024 chars (and naming both ops and setup triggers: deploy/servers.yml/ssh), under 500 lines, and — load-bearing — NO `metadata:` key in the canonical frontmatter. That last pin is what keeps block insertion/removal trivial: the install-time renderer owns the `metadata:` block and `stripOwned` must round-trip back to exactly the canonical bytes, so a hand-authored `metadata:` key would break the byte-equality invariant.

## Package Coupling

The TUI package depends on `runner.Composer` (interface), not `compose.Compose` or `compose.RemoteCompose` (concrete). For operations that need to create a `Composer` at runtime (e.g., project picker selecting a project), use a `ComposerFactory` callback injected from `cmd/` — this keeps `tui` decoupled from `compose`. The factory takes the whole `compose.Project`, not a directory string, so it can branch on `proj.Unmanaged` and return the read-only `compose.HostContainers` for the synthetic picker row. Similarly, `ProjectLoader` and `ConnectCallback` are injected by `cmd/` for remote server support. The `compose` package has the `HasComposeFile` package-level function for local compose file detection; project discovery (`ListProjects`) is a method on `Compose`/`RemoteCompose` requiring `Detect()` first. The `ConfigProvider` and `ExecProvider` interfaces are defined in `tui` (not `runner`) because they return `*exec.Cmd` and are TUI-only; the TUI type-asserts `composer.(ConfigProvider)` and `composer.(ExecProvider)` at runtime — no separate factory or wiring needed. `ReadOnlyComposer` is the same shape and the same reason: only `compose.HostContainers` implements it, and a composer that omits it renders the normal writable screen. `Inspector` is the same shape with the reach inverted — ALL THREE composers implement it, so the `i` key is live on every container screen and the type assert exists for test mocks rather than for a capability split.

## Testing Approach

Tests use stdlib `testing` only — no testify or other test frameworks.

- `internal/runner/`: Mock `Composer` interface to verify step sequences and failure behavior without Docker
- `internal/compose/`: Test command construction (args, env vars, flags) without executing commands; `parseProjects()`, `parseContainerStatus()`, `parseStatsOutput()`, `parseSize()`, `parseCPUPercent()`, and `FormatBytes()` extracted for testability; `RemoteCompose` tests verify SSH arg construction and shell escaping; `inspect_test.go` drives `pickInspectContainer`/`pickHostInspectContainer`, `parsePsEntries` and `ParseInspect` against three real `docker inspect` fixtures, and asserts the inspect argv carries no `compose`; `hostcontainers_test.go` drives `HostContainers` through a fake `dockerRunner` (the same seam production uses), so every read method is testable without Docker and the remote adapter's `SSHExtraArgs`-before-host splice is pinned directly
- `internal/config/`: Test YAML parsing, validation, and lookup with temp files
- `internal/tui/`: Test model state transitions by calling `Update()` with `tea.KeyMsg` directly — no TTY needed; `format_test.go` tests `formatLogContent()`, `softWrapLine()`, and `prettyPrintLine()` as pure functions; `inspect_test.go` tests `buildInspectSummary()` the same way, against the compose package's fixtures; `help_test.go` pins the `?` overlay (the two-directional drift pin — run PER VARIANT, `TestHelpGroups_NamesEveryBoundKey` for the writable table and `TestHelpGroups_ReadOnlyNamesEveryBoundKey` for the read-only one, plus `TestHelpGroups_ReadOnlyDropsGatedKeys` — the `allScreens` completeness check, the typing/confirmation exceptions, and the never-exceeds-width/height clamps)
- `internal/logging/`: Use `t.TempDir()` for file creation tests
- `skills/`: Content pins on the embedded `cdeploy/SKILL.md` (frontmatter first, `name` matches dir + regex, description ≤1024 chars, <500 lines, no `metadata:` key) — see "Agent Skill distribution" above
- `cmd/`: Test flag registration, subcommand existence, and validation via `NewRootCmd()`; `cmd/skill_test.go` uses `t.Setenv` HOME/CODEX_HOME (never `t.Parallel`) for the path matrix, symlink dedupe, and the install/uninstall decision matrix in a temp HOME (render/verify round-trip is pure)
